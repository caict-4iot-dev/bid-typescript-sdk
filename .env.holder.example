# ============================================================
# 持证方 (holder) sample 专用配置
# 持证方只依赖 VC 平台 HTTP 接口：登录、推荐列表、申请、状态、下载、导出。
# 不写链、不解析、不验证 issuer 信任，因此只需一个配置项。
#
# 其余参数（如申请模板、主体、hold）请用 CLI 参数传入：
#   npm run sample:holder -- apply --template-id=... --subject='{"name":"Alice"}'
# ============================================================

# VC 平台主机根地址，不要包含 /server。SDK 内部固定拼接 server/... 路由。
VC_PLATFORM_BASE_URL=
