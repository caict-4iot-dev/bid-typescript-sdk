# ============================================================
# 本地验证方 sample 专用配置
#
# 直连版（sample:verifier）：
#   BID_DIRECT_NODE_URL 用于 IAM/TDS 信任查询和 DDO 合约 queryBid
#   （需配置 BID_BOP_URL/BID_BOP_API_KEY/BID_BOP_API_SECRET 之外的直连地址）。
#
# BOP 开放平台版（sample:verifier-bop）：
#   BID_BOP_URL / BID_BOP_API_KEY / BID_BOP_API_SECRET 用于 IAM/TDS 信任查询
#   与 DDO 合约 queryBid（读 issuer DID 文档与公钥）。
#
# 两种传输共用：
#   VC_REVOCATION_BASE_URL 是发证方平台地址，撤销状态查询该地址下的 /api/cred/vc/revoked/{id}。
#
# 不需要私钥、issuer 公钥、解析服务或平台验证 URL。
# 请使用受信任的 HTTPS 地址；SDK 默认保持 TLS 证书校验。
# ============================================================

# 直连链节点（sample:verifier 使用）
BID_DIRECT_NODE_URL=

# 开放平台（sample:verifier-bop 使用）
BID_BOP_URL=
BID_BOP_API_KEY=
BID_BOP_API_SECRET=

# 发证方平台撤销状态查询根地址（两种传输共用）
VC_REVOCATION_BASE_URL=